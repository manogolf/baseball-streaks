name: Cron Runner

on:
  schedule:
    - cron: "*/113 * * * *" # Runs every 113 minutes
  workflow_dispatch: # ✅ Manual trigger from GitHub UI

jobs:
  run-cron:
    runs-on: ubuntu-latest

    env: # ✅ Inject environment variables securely from GitHub Secrets
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18 # ✅ Match your current environment

      - name: 📥 Install dependencies
        run: npm ci # Faster and more reliable than `npm install` for locked dependencies

      - name: 🚀 Run Cron Script
        run: node scripts/cronRunner.js
